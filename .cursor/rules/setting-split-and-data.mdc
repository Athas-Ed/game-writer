---
description: 长文本设定拆分与 data/ 设定库写入时的约束（防发散、防错误目录）
globs: data/**,skills/setting_splitter/**,config/preferences.md
---

# 设定拆分与资料库

- 用户粘贴「长文本设定集」并要求拆分时：**只做划分与保存**，不要扩写、不要补充情节或世界观、不要写创作建议；格式化（标题、列表）以保持与原文一致为准。
- **规划与脚本**：用 **WriteFile** 手工拆分时先列「条目 → 路径」再写入。用 **RunSkill + setting-splitter** 时以脚本解析为准（宜采用 `一、大节` + `（一）实体名` + `1．小节`）；勿在误拆后批量 DeleteFiles 误删有效内容。
- **标题回声**：`##`/`###` 下若紧跟一行与标题完全相同的正文，整理时删掉（脚本已处理；手工编辑同样适用）。
- 势力类条目默认写入 `data/背景设定/势力/`；地理/场景类写入 `data/背景设定/地点/`；角色写入 `data/角色设定/`。不要使用已废弃目录名 `data/背景设定/阵营/`、`data/背景设定/地图/`（若存在残留空目录或误拆文件，在正确文件已就位后删除）。
- 拆分结束后：**列出 `data/背景设定` 子目录**（或 `SettingsRoute files dir=背景设定`），删除本轮产生的错误嵌套空文件夹与误拆 `.md`；不要删除用户明确要保留的自定义分类。
